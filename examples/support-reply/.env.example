# Copy to the repo-root .env or export these in your shell.
# Never commit real values.

# Gateway origin, with or without /v1.
# Demo (scales to zero): https://kambah123--natlas-serve-natlasservice-serve.modal.run
NATLAS_BASE_URL=

# Bearer key from the gateway's NATLAS_API_KEYS. Not published in this repo.
NATLAS_API_KEY=
