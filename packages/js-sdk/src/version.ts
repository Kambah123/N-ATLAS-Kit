/**
 * Package version.
 *
 * Kept as a literal rather than imported from package.json so the bundle works
 * in browsers and edge runtimes with no JSON-module support. Bump this
 * together with package.json before publishing. See docs/RELEASING.md.
 */
export const VERSION = '0.1.0';
