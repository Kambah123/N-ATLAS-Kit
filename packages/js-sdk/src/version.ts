/**
 * Package version.
 *
 * Kept as a literal rather than imported from package.json so the bundle works
 * in browsers and edge runtimes with no JSON-module support. The release
 * workflow keeps it in sync with package.json.
 */
export const VERSION = '0.0.0';
